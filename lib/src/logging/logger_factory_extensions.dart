import 'logger.dart';
import 'logger_factory.dart';

/// LoggerFactory extension methods for common scenarios.
extension LoggerFactoryExtensions on LoggerFactory {
  /// Creates a new [Logger] instance using the full name of the given [type].
  //Logger createLoggerFromType(Type type)
}
